.recordHeader
  %h2= link_to 'Novy vyber z temat', '#'
.recordPath
.recordEdit

  = error_messages_for :tag_selection

  - form_for @tag_selection, :url => collection_url do |f|
    %fieldset
      = render :partial => "form", :locals => { :f => f }
      %p= submit_tag "Ulozit"