.recordHeader
  %h2= link_to 'Editovat definici víkendu', '#'
.recordPath
.recordEdit
  - form_for @holiday_definition, :url => object_url, :html => { :method => :put } do |f|
    %fieldset
      = render :partial => "form", :locals => { :f => f }
      %p= submit_tag "Upravit"