$(".listFiles.listImgr").append("<%= escape_javascript(render(:partial => 'image', :object => @picture)) %>");
$("a[id^='ajaxImage']").bind("click", function() { return tmpremoveFileFromArticle( $(this).attr("id").split("-")[1], $(this).attr("id").split("-")[2], $(this).attr("id").split("-")[3], $(this).attr("id").split("-")[4], 'remove_image' ) });
