.recordHeader
  %h2= link_to 'Editovat otazku dne', '#'
.recordPath
.recordEdit

  = error_messages_for :dailyquestion

  - form_for @dailyquestion, :url => object_url, :html => { :method => :put } do |f|
    %fieldset
      = render :partial => "form", :locals => { :f => f }
      %p= submit_tag "Upravit"

