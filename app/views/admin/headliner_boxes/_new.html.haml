%h1 New headliner_box

= error_messages_for :headliner_box

- form_for(:headliner_box, :url => collection_url) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Create"

= link_to 'Back', collection_url