%h1 Editing headliner_box

= error_messages_for :headliner_box

- form_for(:headliner_box, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Update"
  
= link_to 'Show', object_url
|
= link_to 'Back', collection_url