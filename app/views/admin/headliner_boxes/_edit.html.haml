.recordHeader
  %h2= link_to 'Editovat headlinovy box', '#'
.recordPath
.recordEdit

  = error_messages_for :headliner_box

  - form_for @headliner_box, :url => object_url, :html => { :method => :put } do |f|
    %fieldset
      = render :partial => "form", :locals => { :f => f }
      %p= submit_tag "Upravit"