$(".recordHeader").remove();
$(".recordPath").remove();
$(".recordEdit").remove();
$("#viewRecord").append("<%= escape_javascript(render(:partial => 'new')) %>");
$("#viewRecord").show();

$("form[id^='new_author']").ajaxForm(
  {
    dataType: 'script',
    beforeSend: function(xhr) {xhr.setRequestHeader("Accept", "text/javascript");},
    resetForm: true,
    complete: function(XMLHttpRequest, textStatus) {
       
    }
  }
);