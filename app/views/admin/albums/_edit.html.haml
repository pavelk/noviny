.recordHeader
  %h2= link_to 'Editovat záznam', '#'
.recordPath
.recordEdit  
  - form_for edit, :url => object_url, :html => { :method => :put, :multipart => true } do |f|
    = f.error_messages
    %fieldset
      = render :partial => "form", :locals => { :f => f }
      %p= submit_tag "Upravit"
    -#= link_to 'Zpět', collection_url