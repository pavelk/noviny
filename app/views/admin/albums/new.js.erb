$(".recordHeader").remove();
$(".recordPath").remove();
$(".recordEdit").remove();
$("#viewRecord").append("<%= escape_javascript(render(:partial => 'new')) %>");
$("#viewRecord").show();

$("form[id^='new_album']").ajaxForm(
  {
    dataType: 'script',
    resetForm: true,
    complete: function(XMLHttpRequest, textStatus) {
       
    }
  }
);