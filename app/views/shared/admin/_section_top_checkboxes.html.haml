- Section.roots.reverse_each do |section|
  - next if section.id == Section::VAS_HLAS or section.id == Section::FORUM
  %div{ :id => "checkboxes-section-top" }
    = check_box_tag :section_ids, section.id, @object.sections.include?(section), :name => "#{return_classname(section_top_checkboxes)}[section_ids][]"
    = label_tag :section_ids, section.name
%br
%br
