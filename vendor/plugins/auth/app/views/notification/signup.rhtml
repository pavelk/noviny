Hello <%= @web_user.name %>,

You required a new account and need to confirm.

The validation key is :

<%= @web_user.validkey %>

You can also visit the following URL :

<%= @app[:url] %>/auth/confirm/<%= @web_user.email %>,<%= @web_user.validkey %>

Thank you,

<%= @app[:title] %> Administrator
<%= @app[:url] %>
